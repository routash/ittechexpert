{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert {{ class | default('') }} alert-{{ label }}">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}